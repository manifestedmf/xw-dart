part of 'numbers.dart';

/// Added in `2.8`.
class Complex extends Num {
  /// Added in `2.8`.
  final int real;
  /// Added in `2.8`.
  final int lateral;

  const Complex(this.real, this.lateral);
}
