part of 'numbers.dart';

/// Added in `2.8`.
sealed class Num {
  /// Added in `2.8`.
  // static Num parse(String text) {}
  /// Added in `2.8`.
  // static Num? tryParse(String text) {}

  const Num();
}