/*import '../../sort.dart' as parent;

class HeapSort {
  static void _heapsort<N extends num>(List<N> A) {
    _buildMaxHeap(A);
    for (int i = A.length; i >= 2; i--) {
      parent.swapElement(A, 1, i);
      A.heapSize--;
      _maxHeapify(A, 1);
    }
  }

  static void _buildMaxHeap<N extends num>(List<N> A) {
    A.heapSize = A.length;
    for (int i = A.length >> 1; i >= 1; i--) {
      _maxHeapify(A, i);
    }
  }

  static
}*/
